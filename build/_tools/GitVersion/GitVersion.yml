branches:
  master:
    regex: ^ma[ster|in]
  feature:
    mode: ContinuousDeployment
  hotfix:
    mode: ContinuousDeployment
  pull-request:
    regex: ^(pull|pull\-requests|pr|patch)[/-]
    mode: ContinuousDeployment